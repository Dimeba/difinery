'use client'

import GTMEvents from '@/components/GTMEvents'
import KlaviyoRefresh from '@/components/KlaviyoRefresh'

const LayoutClientEffects = () => {
	return (
		<>
			<GTMEvents />
			<KlaviyoRefresh />
		</>
	)
}

export default LayoutClientEffects
